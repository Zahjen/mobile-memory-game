import { StyleSheet } from "react-native";

export const gridScreenStyle = StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: "row",
        flexWrap: "wrap",
        justifyContent: "center",
        alignItems: "center"
    }
})